spring:
  profiles:
    active: local
  application:
    name:

  main:
    allow-circular-references: true
  # Database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://ec2-34-200-35-222.compute-1.amazonaws.com:5432/d10bmfrf4pll3v
    username: cjmufnawoddmtx
    password: 1d743961b73f6681cdf8b93b5418db4c0699ad4cc870a839f23dcd72b235ba1e
  # JPA properties
  jpa:
    hibernate:
      ddl-auto: update # When you launch the application for the first time - switch "none" at "create"
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true
logging:
  pattern:
    console: "%d %-5level %logger : %msg%n"
  level:
    org:
      springframework:
        security: DEBUG
        hibernate: DEBUG

server:
  port: 8181 #set your port
jwt:
  secret: secret